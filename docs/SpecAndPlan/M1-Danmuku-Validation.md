# M1 弹幕插件验证记录

依据：[设计](M1-Danmuku-Design.md)、[实施计划](M1-Danmuku-ImplementationPlan.md)。本记录保留各阶段实际验证结果。P5–P9 实施及本地完整自动化验证已完成，P10 人工验收已开始，尚未整体通过。

## P3/P4 提交前回归（2026-09-22）

基线：.NET 10、Jellyfin Server 12.1、Controller/Model 12.1.0；Danmuku 0.1.0、SQLite schema v3。P3/P4 在 P2 基础上新增流式 XML/JSON 解析、导入生命周期、媒体绑定与原子替换，尚未提供完整管理 HTTP API 和播放集合。

| 验证 | 实际结果 |
| --- | --- |
| `./build/test.sh danmuku`，不设置样本目录 | 147 通过、4 显式跳过、0 失败 |
| `DANMUKU_SAMPLE_DIR=<样本目录> ./build/test.sh danmuku` | 151 通过、0 跳过、0 失败 |
| `./build/test.sh agentbridge` | 4 通过、0 失败 |
| 两插件分别 `./build/package.sh <插件>` | 均构建成功，0 警告、0 错误，独立生成 ZIP 与 SHA-256 |
| Danmuku 单独、AgentBridge 单独、两插件共存 smoke | 三组均通过；独立 GUID/加载、管理员 health、匿名拒绝、配置页资源、配置保存及重启持久化；Danmuku schema v3/WAL |

编译、测试、打包顺序执行，设置 `DOTNET_PROCESSOR_COUNT=1`、`MSBUILDDISABLENODEREUSE=1`、`UseSharedCompilation=false`，限制重度任务到一个可用逻辑 CPU。大输入逐文件流式生成，测试后清理；不在内存中拼接完整大文件。原始日志留在忽略目录 `artifacts/m1/p4-submit/`。

Smoke 调用仓库原有三个 scenario，仅在隔离 Compose 中加入单 CPU、1 GiB 内存和 `DOTNET_PROCESSOR_COUNT=1` 限制。固定端口、只读媒体、独立测试数据及原有检查保持不变；运行后清理对应容器和网络，持久化测试证据不入 Git。

## 覆盖内容

- 两种格式真实 300,000/300,001 源条目、52,428,800/52,428,801 字节、零有效条目、跨缓冲文本及超大 JSON 元素结构校验；真实 300,000 条接收、解析、发布全链路及临时文件清理。
- 1–10 文件批次与第 11 个拒绝、全局 100 个位置、最多两路接收、按序发布、空缺位置取消/到期、断连与响应丢失故障注入。
- 内容复用、同名后缀、主动停用；替换的三种生效状态、B 已绑定及 B=A、版本冲突、保留原目标的 Resume、其他媒体引用不变。
- 单文件/批次异常确认、不授权未来异常；24 小时确认期限、终态七天保留、重启中断、取消竞争、清理失败归属与分页重试。
- v2→v3 迁移备份、关联数据保留、失败回滚；媒体缺失与查询失败分别记录，手动全量检查任务计数。

外部样本分别核对设计记录，不将 XML/JSON 当作同一批数据的等价导出。样本不纳入仓库；未配置目录时明确跳过，内容哈希不匹配时失败。

| 样本 | SHA-256 |
| --- | --- |
| `BV1zCtq61Eoi.xml` | `3827a5efb558ce2fd9398139d6a02d5d556104b2176931268f210faccdc945f6` |
| `BV1zCtq61Eoi.json` | `4f3f73b79f4da149b0bd57b522fc3f28e6c2049cd2ceeff1f271bac827862f7b` |
| `BV1cSec6tEux.xml` | `00937d67b6fed630bcd9ea4f5aeef84e75ef8313df70014e503b1765439b6cbd` |
| `BV1cSec6tEux.json` | `3088b2820de74eadd7701b664f2a7a8f43e3e54a37b05a791889d1a5adacc75c` |

## P3/P4 当时未覆盖范围

当时 4 GiB 暂存容量通过持久化预留值触发拒绝，未实写 4 GiB、未测试磁盘耗尽。服务层故障注入不等同于真实 HTTP 上传和权限验证；这些属于 P5。播放集合、完整管理页面、部署入口工具和浏览器性能仍分别属于 P6–P9；本记录不将 P1 的最小叠加层验证当作完整播放器验证。

人工验收尚未开始，待 P9 完成后按实施计划 H01–H10 执行。桌面产品浏览器、全屏、linux-arm64 运行、Synology/SPK 及正式部署均不因上述回归而视为通过。


## P5–P9 最终回归

基线：Danmuku 0.2.0 / AssemblyVersion 0.2.0.0、AgentBridge 0.1.0、Jellyfin Server 12.1、Controller/Model 12.1.0、SQLite schema v3。P5–P8 实现管理 API、权限校验、确定性有限播放集合、磁盘重试缓存、四区管理页面、原生 Canvas 播放器及独立入口工具。两插件 GUID、配置与数据边界保持独立。

| 检查 | 已执行结果 |
| --- | --- |
| Danmuku，不设置外部样本目录 | 164 通过、4 显式跳过、0 失败 |
| Danmuku，四份设计哈希匹配样本 | 168/168 通过 |
| AgentBridge | 4/4 通过 |
| 入口工具单元测试 | 3/3 通过 |
| 两插件独立构建、打包 | 均成功，0 警告、0 错误 |
| Chromium / Firefox 引擎 smoke | 2/2 通过 |
| Danmuku 单独、AgentBridge 单独、共存 smoke | 三组均通过，Danmuku v3/WAL、配置保存与重启、独立性检查通过 |
| 根路径与 `/jellyfin` Base URL HTTP、部署及浏览器回归 | 两种路径均通过；各含 Chromium 与 Firefox |
| 默认/最大配置正式长时间性能矩阵 | 四组全部通过；关闭/开启各 10 分钟，共 80 分钟实际连续播放 |

本次增加的覆盖包括：管理员读写权限、普通用户与受限媒体权限、权限撤销优先于缓存、空上传位置的显式空 taskId、异常分页说明、版本冲突后的 Resume；播放集合的跨用户/会话/媒体隔离、重启和到期返回 410、容量拒绝及分页清理失效会话；管理页上传、下载原文件哈希、异常确认、生效/停用、解绑与删除；暂停、倍速、跳转、迟到响应、偏好隔离及进入退出资源计数。

原始证据位于忽略目录 `artifacts/m1/p9-final/` 与 `artifacts/m1/integration/`；凭据、日志、截图和样本不进入 Git。

### HTTP、浏览器和部署验证

Browser 插件在本次会话不可用，采用固定 Playwright 1.58.2。实际浏览器为 Chromium 145.0.7632.6、Firefox 146.0.1，均 headless、`en-US`，视窗 1440×900 与 390×844；Chromium 禁用 GPU，Firefox 使用默认设置。显式设置 locale 避免测试环境中 Firefox 的原生页面国际化异常。窄视窗不代表移动浏览器验证。

根路径与 `/jellyfin` 分别在新建隔离实例执行真实 HTTP、入口部署和两个浏览器的完整流程：

- 匿名及普通用户不能调用管理接口；普通用户仅能请求有播放权限的媒体，撤销权限后已生成缓存也返回拒绝。检查分页、参数校验、任务状态与空上传位置、跨会话和跨媒体冲突、重启后的 410。
- 真实 socket 中断未完成上传，验证任务失败并释放位置；完整发送请求但不读取响应，另行查询接收成功后断开，再次提交同一位置复用任务。覆盖实际传输故障，不仅依赖服务层注入。
- 管理页面四区切换、配置保存及非法顺序拒绝、恢复默认数值；上传、异常第二页与整批确认、绑定/生效/停用、原文件下载哈希、解绑及删除。
- 三种显示模式确有渲染；暂停画布不变、倍速和向后跳转、控制区 DOM 重建不重新加载集合；桌面和窄屏设置面板边界通过。截图已检查。画中画只模拟进入/退出事件验证清理与恢复，不声称画中画窗口显示已验收。
- 离开页面立即取消待返回的会话查询和播放请求，迟到响应不能覆盖新页面；自动失败不重复请求，手动重试、410 提示及恢复成功；退出登录释放资源、同浏览器换用户偏好隔离。禁用或脚本不可用时原播放器继续工作。
- 入口重复生成、非根路径、未知自定义入口拒绝、撤销和容器重建、保留数据的插件卸载重装均通过。镜像变更测试使用基于 12.1 添加入口探针的派生镜像，验证重新提取及回退流程；这不证明另一个 Jellyfin 版本的兼容性。

浏览器无未处理的 Danmuku 页面异常。原生登录前 WebSocket 连接失败、无 Chromecast 环境警告及故障注入的 503/410/404 保留在证据中；原生 TanStack 导航取消按精确来源单独记录，不作为插件异常，也不笼统忽略其他页面错误。

每种路径、每个浏览器连续进入/退出 20 次。每轮结束 Canvas、监听器、动画帧、观察器、请求、集合及活动弹幕均归零，仅保留全局适配器的一个定时器。以下为第一轮与第二十轮结束值，RSS 是浏览器进程树合计，不是插件专属内存：

| 路径 / 引擎 | JS 堆（字节） | 浏览器 RSS（KiB） |
| --- | --- | --- |
| 根路径 / Chromium | 22,147,494 → 23,477,750 | 852,812 → 970,360 |
| 根路径 / Firefox | 引擎不提供该接口 | 1,102,588 → 1,148,284 |
| `/jellyfin` / Chromium | 23,089,082 → 24,177,417 | 856,796 → 959,752 |
| `/jellyfin` / Firefox | 引擎不提供该接口 | 1,110,176 → 1,158,896 |

Chromium 每轮在相同受控 GC 条件下记录堆，Firefox 结合资源计数和 RSS 趋势。上述有限次数没有发现插件活动资源累积，不据此承诺任意时长或所有浏览器的内存恒定。

### 性能测量条件与边界

正式矩阵使用同一 3,700 秒 H.264 视频（640×360、24 fps、504,649,941 字节），SHA-256 为 `c230c1ff372a0766ab5f13a58f72284d4046b61e30e8f6ea36172dc7bb3165e3`。测试环境为 x86_64、.NET SDK 10.0.112、Node 24.20.0；服务容器限制单 CPU、1 GiB，浏览器任务限制一个逻辑 CPU，重度任务串行。视频只读挂载，无生产数据。

每种配置和视窗分别进行 20 次新播放标识加载、20 次设置响应、20 次跳转重建、20 次进入退出，以及弹幕关闭/开启各连续 10 分钟。加载不复用同一个播放标识的重试缓存；首次应用选择单独记录，不称为操作系统磁盘冷启动。掉帧同时检查实际播放时间、解码帧数和弹幕渲染量，避免空画布取得无效结果。同屏值为配置上限，实际峰值由布局及防碰撞规则决定，结果分开记录。

正式连续播放矩阵四组全部通过（耗时单位 ms，掉帧为百分比）：

| 配置 / 视窗 | 首次测量 | 加载 P95 | 设置 P95 | 跳转 P95 | 关闭 / 开启掉帧 | 增量（百分点） | 开启段渲染 / 活动峰值 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 默认 / 1440×900 | 561.0 | 931.1 | 15.1 | 27.1 | 0 / 0.0903% | 0.0903 | 1,613 / 30 |
| 默认 / 390×844 | 1,538.4 | 557.3 | 12.9 | 14.4 | 0 / 0 | 0 | 737 / 14 |
| 最大 / 1440×900 | 843.9 | 577.5 | 13.6 | 18.4 | 0 / 0.2152% | 0.2152 | 3,046 / 49 |
| 最大 / 390×844 | 817.4 | 608.1 | 13.7 | 11.1 | 0 / 0 | 0 | 992 / 18 |

上述首组前的首次应用选择为 1,537 ms、10,000 条。首组关闭/开启段分别实际播放 600.045 / 600.078 秒，各解码 14,402 帧。默认窄屏关闭/开启段分别实际播放 600.026 / 600.060 秒，解码 14,401 / 14,402 帧。最大桌面关闭/开启段分别实际播放 600.036 / 600.091 秒，各解码 14,402 帧。最大窄屏关闭/开启段分别实际播放 600.037 / 600.041 秒，各解码 14,402 帧。四组加载 P95 均低于 2,000 ms，设置/跳转 P95 均低于 200 ms，最大掉帧增量为 0.2152 个百分点，低于 1 个百分点。首次启动在计时前遇到测试脚本与原播放器切换视频源的竞态，已改为等待原播放器实际开始播放；失败启动不计入正式测量。

合成弹幕源为 300,000 条、28,898,772 字节，SHA-256 为 `bc607d7a43110c0a0cb817c17908a625ac51df7251a5fa33aa88c90d40304558`。最大配置使用 20,000 条集合、100 条同屏上限；防碰撞及窄屏布局使实际峰值分别为 49、18，未验证持续 100 条同时显示的负载。此处“最大配置通过”指配置合法、集合足量且上述实际场景达标。

性能矩阵另含每场景 20 次、总计 80 次进入退出，全部资源计数恢复基线。相同 GC 条件下 JS 堆第一轮 → 第二十轮为：默认桌面 21,207,843 → 22,735,238 字节；默认窄屏 23,380,770 → 23,940,203；最大桌面 24,001,985 → 24,458,825；最大窄屏 24,730,476 → 25,082,663。对应浏览器进程树 RSS 为 900,872 → 1,020,440、913,728 → 1,007,736、1,017,980 → 1,154,472、976,700 → 1,055,360 KiB。资源计数无累积，堆增量随场景减小；浏览器自身缓存及 RSS 保留仍存在，不将它们归零作为验收条件。

最终证据：`artifacts/m1/p9-final/` 保存单元、样本、入口、构建打包和三场景 smoke 日志；根路径及非根路径的完整回归日志为 `browser-root-final.log`、`browser-base-final.log`。正式性能日志为 `performance.log`，退出码为 0；完整逐次指标、截图和 HTTP/部署结果保存在对应 `artifacts/m1/integration/` 隔离测试目录。最终完整 Web 回归之后插件运行源码保持不变，浏览器脚本仅修正原播放器启动等待并改进证据脱敏及流式哈希；长测使用该最终脚本。未把早期失败启动或预检计为正式通过。

两份最终发布包的校验文件匹配，包内未混入另一插件或 Jellyfin Controller/Model。Danmuku `0.2.0` ZIP SHA-256 为 `849b6b7750b02671a475a0583f65c3cbc3bc3154d4a618bebb42333788035fd9`，AgentBridge `0.1.0` 为 `8adc0d4f5b20051e16ca08c28ed5e985118ff49efad0a17abffc2c7e96e8096d`。这是本次产物校验值，不承诺不同构建环境生成相同 ZIP 字节。

### 复现及剩余人工范围

按部署文档安装固定版本浏览器工具和 FFmpeg 后，依次执行 `./build/test.sh danmuku`、`./build/test.sh agentbridge`、`python3 -m unittest discover -s tests/integration -p 'test_*.py'`、两插件独立打包、`./build/smoke-test.sh`、`./build/test-browser.sh`、`./build/test-browser.sh --base-url /jellyfin`、`./build/test-performance.sh`。外部设计样本通过 `DANMUKU_SAMPLE_DIR` 提供并运行 `./build/test-danmuku-samples.sh`；未提供时不能把四项跳过当作通过。所有服务检查顺序占用固定 18096 端口。

P10 已开始，H01–H10 尚未逐项完成确认，特别是实际 Chrome/Edge、桌面全屏和主观播放体验。未实写 4 GiB 或进行磁盘耗尽测试；linux-arm64 原生资产已打包但未运行，Synology/SPK、Safari、移动浏览器和正式部署未验证。自动化样例以电影为主，不把服务层跨媒体测试等同于所有电视剧层级的人工操作验收。


## 首次推送后的 CI 回归修复

`b034146` 的 GitHub CI 中，两插件独立构建/测试/打包及三场景 smoke 通过；Danmuku 集成任务通过 HTTP、部署检查后，Chromium 在配置页非法顺序校验时超时，非根路径步骤未执行。失败截图显示原输入已被配置加载结果覆盖，不能把该次远端 CI 记为通过。

修复管理表单的异步加载竞态：所有配置加载及保存完成前禁用字段和提交按钮；保存先固定用户输入快照，再等待配置请求。新增浏览器用例主动拦住配置响应，检查加载中不可编辑、加载完成后可编辑、非法顺序拒绝及合法保存。回归还发现 Firefox 对相同 hash 地址可能只执行同文档导航，脚本缺失用例现使用显式整页刷新，符合原验收的刷新要求。

修复后根路径与 `/jellyfin` 非根路径的 HTTP、部署及 Chromium/Firefox 完整回归均通过，含主动延迟配置响应和显式刷新检查。本次重新构建并打包 Danmuku，0 警告、0 错误。仅配置页及浏览器测试发生变化，未重复执行播放性能长测，前文 80 分钟测量和两份 ZIP 哈希仍是 `b034146` 对应的历史证据。原始修复回归记录保存在忽略目录 `artifacts/m1/ci-fix/`。


## P10 播放控件布局反馈

用户已实际导入、绑定并播放视频，截图显示弹幕可见，但弹幕文字按钮单独占一行。按用户反馈，将入口改为原生按钮行内、紧邻收藏爱心左侧的 SVG 气泡图标，保留“弹幕设置”悬停提示、无障碍名称及展开状态。控件重建继续移动原节点，不重载播放集合；设置面板按视窗边界定位。固定 Web 资源版本升至 `m1-v2`，筛选算法版本不变。

本次插件契约测试 6/6，通过构建及打包；根路径及 `/jellyfin` 非根路径的 HTTP、部署和 Chromium/Firefox 完整回归均通过。新增检查覆盖图标无可见文字、原生按钮行内紧邻爱心、同排坐标及图标尺寸、控件重建后的顺序、桌面和窄屏面板边界。截图已检查。回归脚本另等待原生首页加载及动画帧完成再进入播放，避免测试快速导航触发 Jellyfin 首页滚动组件的初始化/销毁竞态，未放宽页面异常断言。

原始证据保存在忽略目录 `artifacts/m1/control-placement/` 和对应 `artifacts/m1/integration/` 目录。本次未重新执行 80 分钟性能矩阵，不把局部界面回归称为整套性能重测。用户已确认此项播放页面改动通过；不将 P10 整体标为通过。

## P10 弹幕显示开关优化

用户确认上一项播放页面布局通过后，要求将“显示弹幕”下拉框改为 switch。现使用带无障碍名称的 checkbox switch，支持鼠标点击、键盘空格与焦点提示；空格只操作当前开关，防止播放器快捷键接管，且不依赖会被 Firefox 下原生全局处理取消的模拟点击。偏好保存与重建渲染沿用原行为。资源版本升至 `m1-v3`。

复核已确认设计发现初始显示区域误为 100%，现将初始及非法值回退改为设计规定的 50%，保留用户已保存的合法选择。密度仍为低、中、高三档（管理员默认配置 15/30/50 条），区域仍从有效视频画面顶部向下延伸；两项设计语义未调整。

本次插件契约测试 6/6、构建和打包通过；根路径及 `/jellyfin` 非根路径的 HTTP、部署和 Chromium/Firefox 完整回归通过。浏览器新增覆盖默认 50% 区域、switch 初始开启、键盘焦点与关闭、鼠标开启；既有播放、偏好和反复退出清理回归继续通过。已检查窄屏截图。测试登录后补齐原生首页加载及动画帧等待，避免立即跳转配置页引起的原生滚动组件生命周期竞态，未放宽页面错误断言。

原始证据位于忽略目录 `artifacts/m1/switch/` 及对应集成测试目录，保留修复前失败记录。本次未重跑 80 分钟性能矩阵，也未新增字幕共存专项验证。开关改动待用户人工复测，不将 P10 整体标为通过。

## P10 显示区域默认值调整为 75%

按用户明确要求，将初始显示区域及非法值回退由 50% 改为 75%，保留用户已有合法选择，资源版本升至 `m1-v4`。设计文档同步更新默认值，并记录密度新设计的逐项讨论结论和未决事项；本次未修改密度实现。

本次插件契约测试 6/6、构建和打包通过；根路径 HTTP、部署及 Chromium/Firefox 完整浏览器回归通过，包含新用户默认 75% 的检查。证据位于忽略目录 `artifacts/m1/default-area/` 及对应集成测试目录。本次未重跑非根路径矩阵和 80 分钟性能矩阵，不把既往结果记为本次执行结果。开发部署保留持久化媒体、弹幕文件、绑定及配置；新默认值待用户人工复测。

## 四档动态密度修订（自动化及数据保留部署通过，待人工复测）

用户在设计及实施计划复审通过后另行明确授权实施，要求实施阶段暂缓提交推送；用户现已确认密度显示正常并授权提交，本次不推送。D1–D5 的实现和必要自动化已通过，D6 的数据保留部署已完成；用户随后确认密度显示正常并授权提交；D7 其他人工项目尚未逐项完成确认，不将 M1 整体标记为验收通过。资源版本为 `m1-v5`，播放渲染契约为 `m1-density-v1`，抽样算法仍为 `m1-v1`；Danmuku 0.2.0、AgentBridge 0.1.0、GUID 和依赖锁文件不变。前文旧三档性能数据只作为历史记录。

### 实现与功能验证

实现低／中／高／重叠四档，默认高；低／中预留完整文字包围盒面积预算为 40%／65%，高取消面积预算但仍防碰撞，重叠允许滚动相互覆盖及覆盖固定弹幕，固定弹幕彼此始终防重叠。密度不改变上下行距，保护上限默认 100／200／400／600，四档全部生效。管理员保存须完整提供四个 1–600 的非递减整数；既有 XML 缺项使用新默认值，正在播放的集合保持配置快照。新密度偏好使用 `densityModeV2`，不读取旧 `density`；区域、字号、透明度和开关偏好保留，无区域偏好时默认 75%。

按原始时间和稳定顺序进行确定性排布，使用视频时钟、完整寿命碰撞判断和跳过策略，不延迟补播。文字测量与索引构建分批调度；首轮加载测量基础字体，换字号按相同字体坐标系缩放，描边保持 2 CSS 像素。单布局键保存决定索引，seek 从已作出的源事件决定还原；不保存多套视窗布局或逐帧快照。操作完成须匹配播放标识、操作、代次和目标进度，并在所有批次及绘制提交完成后发出，取消或迟到失败不能覆盖新终态。

本轮已执行并通过：

- Danmuku 无外部样本：165 通过、4 显式跳过；提供设计样本后 169/169。AgentBridge 4/4，入口工具 3/3。
- 两插件独立构建、打包及三个隔离加载 smoke 场景通过。后续仅前端及浏览器测试细化，最终 Danmuku 包重新构建，并用于下列完整回归、正式性能和部署；未将前一包的 smoke 写成最终包另跑了一遍 smoke。
- Node 纯布局及引导测试 10/10，覆盖预算释放、混合模式、完整寿命追尾、非法／过长尺寸、上限 1 与 600、20,000 条连续／冷跳转／逆向重建一致性、256 条／4 ms 批次边界、独立轨迹碰撞检查及后续资源版本引导。
- 根路径与 `/jellyfin` 各自真实 HTTP、部署、Chromium 和 Firefox 全套功能通过，包含配置完整性与原子拒绝、409 版本契约、权限、默认值、四档交互、字幕共存与原生字幕切换、Fullscreen API、桌面／窄屏布局、退出清理、20,000 条加载及 600 条峰值。
- 旧 Bootstrap 固定原始字节配合新资源，在两条路径检查共享加载去重、超时／失败／错误版本和普通刷新恢复。缓存命中采用路由模拟，不声称验证了浏览器真实 HTTP 缓存替换。
- 同一提交观察器验证多批重建、A/B 取消、空画面完成、字体尺寸回退、控件重建及禁用后迟到失败隔离。人为延迟最后一批的 20 次负向样本被正式 P95 判定拒绝，第一帧未误报完成；负向数据不混入正式成绩。

Browser 插件不可用，沿用固定 Playwright 1.58.2；功能覆盖 Chromium／Firefox。根路径最终证据为 `artifacts/m1/integration/jellyfin-forge-m1-72d45611fc/`，非根路径为 `artifacts/m1/integration/jellyfin-forge-m1-8c770119a2/`；逐操作原始数据保存在 `*-density-results.json`，截图已检查。完整自动化日志保存在 `artifacts/m1/density/`。没有运行本轮远端 CI，因为尚未提交推送；用户报告的既有 CI 通过对应实施前基线。

### 正式性能矩阵

重度任务串行，编译限制 .NET 并发、禁用节点复用和共享编译；服务容器限 1 CPU／1 GiB，浏览器任务限制一个逻辑 CPU。性能使用 Chromium 145.0.7632.6、headless、关闭 GPU 硬件加速，四组均在真实视频时钟下运行关闭／开启各连续 10 分钟，总计 80 分钟。默认集合 10,000 条、高档保护 400；最大集合 20,000 条、重叠保护 600；区域 75%、字号 100%。不降低批准上限来满足门槛。

每组首次真实加载单列，另取 20 个新播放标识的加载样本、20 次设置重建、20 次 seek 和 20 次进入退出。加载包含请求、解析、全部文字测量、事件前缀重演、批次等待及绘制；设置从输入处理前计时，seek 从 `seeked` 计时至对应有效代次提交。不是裸 HTTP、单次 RAF 或上一操作完成时间。首次应用运行不等同于操作系统磁盘冷启动。

正式矩阵全部通过；耗时为 ms，掉帧增量为百分点：

| 配置／视窗 | 首次加载 | 加载 P95 | 设置 P95 | seek P95 | 关闭／开启掉帧 | 增量 | 开启段实际绘制／活动峰值 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 默认／高 1440×900 | 947.1 | 1393.0 | 17.0 | 5.6 | 0.0000% / 0.0903% | 0.0903 | 1,585 / 31 |
| 默认／高 390×844 | 826.0 | 720.4 | 11.5 | 4.0 | 0.0000% / 0.0000% | 0.0000 | 608 / 14 |
| 最大／重叠 1440×900 | 865.1 | 774.7 | 78.5 | 34.4 | 0.0000% / 0.1736% | 0.1736 | 6,785 / 600 |
| 最大／重叠 390×844 | 858.2 | 733.9 | 69.4 | 23.0 | 0.0000% / 0.0000% | 0.0000 | 6,304 / 600 |

默认／高 1440×900 关闭／开启分别播放 600.053／600.097 秒、解码 14,401／14,402 帧；默认／高 390×844 关闭／开启分别播放 600.067／600.087 秒、解码 14,403／14,402 帧；最大／重叠 1440×900 关闭／开启分别播放 600.078／600.157 秒、解码 14,401／14,404 帧；最大／重叠 390×844 关闭／开启分别播放 600.090／600.113 秒、解码 14,403／14,404 帧。

最大桌面／窄屏均达到 600 条峰值，连续不少于 570 条分别维持 53.015／53.004 秒，满足至少 30 秒的要求；1 Hz 活跃时序保留在原始结果。最大新增数值缓冲为 1,160,312 字节，低于 2 MiB，仅是本功能索引／计数缓冲，不代表浏览器总内存。

四组加载 P95 均小于 2,000 ms，设置／seek P95 均小于 200 ms，最大掉帧增量 0.1736 个百分点，小于 1 个百分点。正式最大桌面的首次末尾冷 seek 为 200.4 ms；功能回归同类冷 seek 也有 260.3／351.0 ms 样本。原始值均保留，批准门槛是 P95，不声称每次冷 seek 都低于 200 ms。长时性能仅覆盖上述 Chromium 条件，不外推为所有浏览器和硬件的性能保证。

每组 20 次进入退出，共 80 次，Canvas、监听器、RAF、观察器、请求、集合、活动条目和数值索引均恢复释放状态，保留全局适配器一个定时器。相同受控 GC 条件下第一轮／第二十轮如下；RSS 为浏览器进程树总量，不是插件独占内存：

| 配置／视窗 | JS 堆（字节） | 浏览器 RSS（KiB） |
| --- | --- | --- |
| 默认／高 1440×900 | 23,927,291 → 24,203,968 | 1,001,248 → 1,109,484 |
| 默认／高 390×844 | 27,906,073 → 25,942,768 | 1,036,004 → 1,092,852 |
| 最大／重叠 1440×900 | 34,830,916 → 27,147,416 | 1,124,532 → 1,203,836 |
| 最大／重叠 390×844 | 27,365,610 → 27,835,599 | 1,104,332 → 1,168,660 |

浏览器缓存及 RSS 保留仍存在；本轮没有发现插件活动资源累积，不据有限轮次承诺任意时长内存恒定。

视频沿用 3,700 秒、640×360、24 fps H.264 隔离样本，SHA-256 为 `c230c1ff372a0766ab5f13a58f72284d4046b61e30e8f6ea36172dc7bb3165e3`。默认合成源 300,000 条／28,898,772 字节，SHA-256 为 `bc607d7a43110c0a0cb817c17908a625ac51df7251a5fa33aa88c90d40304558`。最大源精确 20,000 条／1,908,616 字节，SHA-256 为 `1178afc5ec5913d52e9a2f42aa5ff7a47044f88eece2cc8e3c9bccb5e58328e2`；确定性生成规则见 `tests/browser/m1.cjs`，其中 4,800 条滚动弹幕以每秒 80 条分布于 120–180 秒，余下条目覆盖视频全长和混合模式。长测各段从 120 秒开始；另有首次直达 3,690 秒的冷前缀 seek，未绕过保护逻辑强塞弹幕。

正式原始结果为 `artifacts/m1/integration/jellyfin-forge-m1-a1e7160db1/chromium-performance.json`，含逐次身份／起止／终态、实际活动时序、掉帧和清理记录；`artifacts/m1/density/performance-exit` 为 0。短时预检和早期失败探索没有计作正式矩阵通过。

最终 Danmuku ZIP SHA-256 为 `29c0dc2d8341f49424a60e76e615c6c0af3c3d81bc573bf937b47d4eeacd3fb6`。源码／测试／构建文件清单保存在 `artifacts/m1/density/source-manifest.json`，清单文件 SHA-256 为 `ee70ddd492323e9fd3532bdc6ed66438829bc81f744675d502ddd1a38138b098`；最终功能回归、长测及部署之间核对清单无变化。部署时程序文件与包匹配，四个 Web 资源实际 HTTP 响应与源码哈希一致；Jellyfin 启动仅在 `meta.json` 添加空 overview/changelog/assemblies 和默认 timestamp，原包元数据字段不变。

### 数据保留部署与人工复测

必要自动化通过后，停止本仓库开发实例，保存旧插件程序、原配置、原始弹幕和 SQLite 一致性备份，检查备份完整性及原文件哈希，再替换已测包并沿用既有镜像重建启动。部署后 health、启用状态、资源版本、固定开发端口、1 CPU／1 GiB 限制和媒体只读挂载均核对通过。账号及媒体配置未重置，未清理持久化目录或触碰其他项目资源。

部署前后 1 个弹幕文件、4,485 条标准化内容、1 条媒体绑定、生效选择及停用标记、原始文件哈希、插件配置完全一致。备份和脱离版本管理的核对结果位于 `artifacts/m1/density/deployment/`。没有模拟破坏现有业务数据来测试回退，也没有将备份可读性检查描述为完整灾难恢复演练。

D7 请在既有测试视频上逐项人工确认，记录实际浏览器版本与结果：

1. 普通刷新后进入播放，确认图标、switch、默认高档；无区域偏好时为 75%，已有区域偏好仍保留。
2. 在同一密集片段切换低／中／高／重叠，结合窗口大小和字号查看占用差异；数量上限不是保证显示数量。
3. 使用含滚动、顶部及底部的混合样本，确认重叠档滚动可覆盖固定，固定彼此仍防碰撞；真实文件缺某种模式时记录未覆盖。
4. 测试暂停、倍速、前后拖动、窗口／全屏和原生字幕切换，确认字幕与弹幕共存、定位稳定、无延迟补播。
5. 保存管理员四档上限，验证旧播放保持快照、新播放采用新值，并确认文件及绑定仍在。

用户已确认当前视频的密度显示正常，并授权提交。其余人工步骤未逐项记录，不据此宣称 M1 全部验收完成。实际 Chrome／Edge 的主观体验、真实 HTTP 缓存升级、Safari／移动设备、Synology/SPK 及生产部署不在本轮已通过范围内；不将 headless 自动化等同于这些人工验收。

用户人工复测补充：用户自行重新上传并绑定了正确的 1,065 条文件。只读统计前 60 秒共 76 条（滚动 41、顶部 35）；整段加载上限 5,000 与高档同屏保护 400 独立，未把保护值用于整段抽样。用户随后确认显示正常。本次排查未改程序或恢复旧业务数据；前述 4,485 条部署前后核对仍是当时的历史事实。
